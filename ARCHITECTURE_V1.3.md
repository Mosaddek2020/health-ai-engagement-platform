# 🏗️ System Architecture: V1.3

## Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │   Browser    │  │   Browser    │  │   Browser    │        │
│  │    Tab 1     │  │    Tab 2     │  │    Tab 3     │        │
│  │              │  │              │  │              │        │
│  │  React App   │  │  React App   │  │  React App   │        │
│  │  (Vite)      │  │  (Vite)      │  │  (Vite)      │        │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘        │
│         │                 │                 │                  │
│         └─────────────────┴─────────────────┘                  │
│                           │                                     │
│                  ┌────────┴────────┐                          │
│                  │                 │                          │
└──────────────────┼─────────────────┼──────────────────────────┘
                   │                 │
          HTTP API │                 │ WebSocket (ws://)
          (port 80)│                 │ (port 8080)
                   │                 │
┌─────────────────────────────────────────────────────────────────┐
│                      APPLICATION LAYER                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────┐    ┌──────────────────────┐          │
│  │   Nginx Server      │    │  Laravel Reverb      │          │
│  │   (port 80)         │    │  WebSocket Server    │          │
│  │                     │    │  (port 8080)         │          │
│  │  Routes HTTP to:    │    │                      │          │
│  │  - Laravel API      │    │  Broadcasts:         │          │
│  └──────────┬──────────┘    │  - Process events    │          │
│             │                │  - Reset events      │          │
│             ▼                │  - Confirm events    │          │
│  ┌─────────────────────┐    │  - Skip events       │          │
│  │   Laravel App       │    └──────────┬───────────┘          │
│  │   (PHP 8.2)         │               │                      │
│  │                     │◄──────────────┘                      │
│  │  Controllers:       │    broadcast()                       │
│  │  - DashboardCtrl    │                                      │
│  │                     │                                      │
│  │  Events:            │                                      │
│  │  - AppointmentsUpd  │                                      │
│  │                     │                                      │
│  │  Commands:          │                                      │
│  │  - ProcessAppts     │                                      │
│  └──────────┬──────────┘                                      │
│             │                                                  │
│             │ Eloquent ORM                                     │
│             │                                                  │
└─────────────┼──────────────────────────────────────────────────┘
              │
              │ Database Queries
              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        DATA LAYER                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────┐          │
│  │   PostgreSQL Database (port 5432)                │          │
│  │                                                   │          │
│  │   Tables:                                         │          │
│  │   ┌────────────────────────────────────────┐    │          │
│  │   │ appointments                           │    │          │
│  │   ├────────────────────────────────────────┤    │          │
│  │   │ id, patient_name, patient_phone        │    │          │
│  │   │ appointment_time, appointment_type     │    │          │
│  │   │ provider_name, status, no_show_risk    │    │          │
│  │   │ created_at, updated_at                 │    │          │
│  │   └────────────────────────────────────────┘    │          │
│  │                                                   │          │
│  └───────────────────────────────────────────────────┘          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

              ┌───────────────────────────────┐
              │   AI Service Layer            │
              ├───────────────────────────────┤
              │                               │
              │  ┌──────────────────────┐    │
              │  │ FastAPI AI Agent     │    │
              │  │ (Python 3.11)        │    │
              │  │ (port 8000)          │    │
              │  │                      │    │
              │  │ Endpoints:           │    │
              │  │ /predict             │    │
              │  │ /predict/no-show     │    │
              │  │ /health              │    │
              │  └──────────────────────┘    │
              │                               │
              └───────────────────────────────┘
```

---

## Request Flow Diagrams

### 1. User Processes Appointments (V1.2 WebSocket Flow)

```
User (Tab 1)                React App           Laravel API         Reverb Server       React Apps (All Tabs)
     │                          │                     │                    │                      │
     │  Click "Process"         │                     │                    │                      │
     ├─────────────────────────>│                     │                    │                      │
     │                          │  POST /process-     │                    │                      │
     │                          │  appointments       │                    │                      │
     │                          ├────────────────────>│                    │                      │
     │                          │                     │                    │                      │
     │                          │                     │ Artisan:call()     │                      │
     │                          │                     │ 'appointments:     │                      │
     │                          │                     │  process'          │                      │
     │                          │                     │                    │                      │
     │                          │                     │ Update DB          │                      │
     │                          │                     │ (30 appointments)  │                      │
     │                          │                     │                    │                      │
     │                          │                     │ broadcast(         │                      │
     │                          │                     │  AppointmentsUpd)  │                      │
     │                          │                     ├───────────────────>│                      │
     │                          │                     │                    │                      │
     │                          │  Success Response   │                    │ Broadcast to all     │
     │                          │<────────────────────┤                    │ connected clients    │
     │                          │                     │                    ├─────────────────────>│
     │  Toast: "Success!"       │                     │                    │                      │
     │<─────────────────────────┤                     │                    │   WS: appointments   │
     │                          │                     │                    │   .updated event     │
     │                          │  WS Event:          │                    │                      │
     │                          │  appointments.      │                    │                      │
     │                          │  updated            │                    │                      │
     │                          │<─────────────────────────────────────────┤                      │
     │                          │                     │                    │                      │
     │                          │ invalidateQueries() │                    │  invalidateQueries() │
     │                          │ (kpi, appointments, │                    │  (all data)          │
     │                          │  action-queue)      │                    │                      │
     │                          │                     │                    │                      │
     │                          │ GET /kpi-stats      │                    │  GET /kpi-stats      │
     │                          ├────────────────────>│                    ├─────────────────────>│
     │                          │ GET /appointments   │                    │ GET /appointments    │
     │                          ├────────────────────>│                    ├─────────────────────>│
     │                          │ GET /action-queue   │                    │ GET /action-queue    │
     │                          ├────────────────────>│                    ├─────────────────────>│
     │                          │                     │                    │                      │
     │  UI Updates              │ Fresh Data          │                    │ Fresh Data           │
     │  (new risk scores)       │<────────────────────┤                    │<─────────────────────┤
     │<─────────────────────────┤                     │                    │                      │
     │                          │                     │                    │  UI Updates          │
     │                          │                     │                    │  (all tabs!)         │
     │                          │                     │                    │<─────────────────────┤
```

**Key Points:**
- Single API call triggers processing
- Broadcast sent to ALL connected clients
- Each client refetches data independently
- UI updates within 100ms across all tabs

---

### 2. Manual Confirm Appointment (V1.3 Interactive Flow)

```
User                    React App           Laravel API         Reverb Server       Other Users
 │                          │                     │                    │                  │
 │  Click "✓ Confirm"       │                     │                    │                  │
 │  on Patient #7           │                     │                    │                  │
 ├─────────────────────────>│                     │                    │                  │
 │                          │  POST /appointments │                    │                  │
 │                          │  /7/confirm         │                    │                  │
 │                          ├────────────────────>│                    │                  │
 │                          │                     │                    │                  │
 │                          │                     │ UPDATE appointments│                  │
 │                          │                     │ SET status='Conf'  │                  │
 │                          │                     │ WHERE id=7         │                  │
 │                          │                     │                    │                  │
 │                          │                     │ broadcast(         │                  │
 │                          │                     │  "Appointment #7   │                  │
 │                          │                     │   confirmed")      │                  │
 │                          │                     ├───────────────────>│                  │
 │                          │  Success Response   │                    │                  │
 │                          │<────────────────────┤                    │ Broadcast        │
 │                          │                     │                    ├─────────────────>│
 │  Toast: "✓ Confirmed     │                     │                    │                  │
 │   for John Doe"          │                     │                    │  WS Event        │
 │<─────────────────────────┤                     │                    │                  │
 │                          │  WS Event           │                    │                  │
 │                          │<─────────────────────────────────────────┤                  │
 │                          │                     │                    │                  │
 │                          │ Refetch all data    │                    │  Refetch data    │
 │                          ├────────────────────>│                    ├─────────────────>│
 │                          │                     │                    │                  │
 │  Patient removed from    │ Updated data        │                    │  Updated data    │
 │  Action Queue            │<────────────────────┤                    │<─────────────────┤
 │  "Confirmed" count +1    │                     │                    │                  │
 │<─────────────────────────┤                     │                    │  Their UI updates│
 │                          │                     │                    │  automatically!  │
 │                          │                     │                    │<─────────────────┤
```

**Key Points:**
- Instant action on button click
- Database updated immediately
- Broadcast ensures all users see change
- Action queue dynamically updates
- No manual refresh needed

---

## Data Flow: V1.1 (Polling) vs V1.2 (WebSocket)

### V1.1 - Polling Architecture (OLD)
```
┌──────────────┐
│  React App   │
│              │
│  useQuery({  │
│   refetch-   │───┐
│   Interval:  │   │
│   5000ms     │   │
│  })          │   │
└──────────────┘   │
                   │
        Every 5 seconds:
                   │
                   ▼
         ┌─────────────────┐
         │  GET /kpi-stats │
         │  GET /appoints  │──────> Laravel API
         │  GET /queue     │
         └─────────────────┘
                   │
                   ▼
         ┌─────────────────┐
         │  PostgreSQL     │
         └─────────────────┘

Problems:
- 36 requests/minute (wasteful)
- 0-5 second delay for updates
- No multi-user sync
- High server load
```

### V1.2 - WebSocket Architecture (NEW)
```
┌──────────────┐
│  React App   │
│              │
│  useQuery({  │───┐ Initial load only
│   NO refetch │   │
│   interval   │   │
│  })          │   ▼
│              │  ┌─────────────────┐
│              │  │  GET /kpi-stats │
│  useEffect({ │  │  GET /appoints  │──> Laravel API
│   echo       │  │  GET /queue     │
│   .listen()  │  └─────────────────┘
│  })          │
└──────┬───────┘
       │
       │ WebSocket Connection
       │ (ws://localhost:8080)
       │
       ▼
┌─────────────────┐
│ Reverb Server   │<────── broadcast() from Laravel
│                 │
│ Channel:        │
│ 'appointments'  │
│                 │
│ Broadcasts:     │
│ - process       │
│ - reset         │
│ - confirm       │
│ - skip          │
└─────────────────┘
       │
       │ Event: appointments.updated
       │
       ▼
┌──────────────┐
│  React App   │
│              │
│  Receives    │
│  event       │
│              │
│  invalidate- │
│  Queries()   │
│              │
│  Refetch     │
│  fresh data  │
└──────────────┘

Benefits:
- 1-5 requests/minute (95% reduction)
- <100ms update latency
- Automatic multi-user sync
- Minimal server load
- Event-driven updates
```

---

## Component Interaction Matrix

| Component | Communicates With | Protocol | Purpose |
|-----------|-------------------|----------|---------|
| React App | Nginx | HTTP | API requests |
| React App | Reverb | WebSocket | Real-time events |
| Nginx | Laravel | FastCGI | Route HTTP to PHP |
| Laravel | PostgreSQL | TCP/5432 | Database queries |
| Laravel | Reverb | Broadcast | Send events |
| Laravel | AI Agent | HTTP | Get predictions |
| Reverb | React Apps | WebSocket | Push updates |

---

## Docker Network Topology

```
┌────────────────────────────────────────────────┐
│     Docker Network: health-ai-network          │
│                  (Bridge Driver)               │
├────────────────────────────────────────────────┤
│                                                │
│  ┌──────────────┐  ┌──────────────┐          │
│  │  postgres    │  │  ai-agent    │          │
│  │  :5432       │  │  :8000       │          │
│  └──────────────┘  └──────────────┘          │
│                                                │
│  ┌──────────────┐  ┌──────────────┐          │
│  │  laravel     │  │  reverb      │          │
│  │  (internal)  │  │  :8080       │          │
│  └──────────────┘  └──────────────┘          │
│                                                │
│  ┌──────────────┐  ┌──────────────┐          │
│  │  nginx       │  │  react       │          │
│  │  :80         │  │  :5173       │          │
│  └──────────────┘  └──────────────┘          │
│                                                │
└────────────────────────────────────────────────┘
         │                        │
         │ Exposed Ports          │
         ▼                        ▼
    Host:80                  Host:5173
    Host:8080                Host:8000
                              Host:5432
```

---

## Event Broadcasting Sequence

```
Action Trigger:
  User clicks button
       │
       ▼
  Laravel Controller
       │
       ├──> Update Database
       │
       └──> broadcast(new AppointmentsUpdated($message, $type))
                │
                ▼
       AppointmentsUpdated Event
                │
                ├──> implements ShouldBroadcast
                ├──> channel: 'appointments'
                ├──> broadcastAs: 'appointments.updated'
                └──> payload: {message, type}
                     │
                     ▼
            Laravel Broadcast
                     │
                     ▼
            Reverb Server
                     │
                     ├──> Connected Client 1 (Tab 1)
                     ├──> Connected Client 2 (Tab 2)
                     ├──> Connected Client 3 (Tab 3)
                     └──> Connected Client N (Tab N)
                          │
                          ▼
                  React useEffect Listener
                          │
                          ├──> console.log()
                          ├──> invalidateQueries()
                          └──> setProcessMessage()
                               │
                               ▼
                          UI Updates Automatically
```

---

## State Management Flow

```
Initial Page Load:
┌─────────────────────────────────────┐
│ 1. React App Mounts                 │
│    - useQuery fetches initial data  │
│    - useEffect sets up WS listener  │
└─────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────┐
│ 2. Display Initial Data             │
│    - KPI Cards                      │
│    - Appointments Table             │
│    - Action Queue                   │
└─────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────┐
│ 3. Wait for User Action or Event    │
└─────────────────────────────────────┘
              │
              ├──> User Click
              │         │
              │         ▼
              │    API Call
              │         │
              │         ▼
              │    DB Update
              │         │
              │         ▼
              │    Broadcast
              │
              └──> WebSocket Event
                        │
                        ▼
              ┌─────────────────────┐
              │ invalidateQueries() │
              └─────────────────────┘
                        │
                        ▼
              ┌─────────────────────┐
              │ Refetch from API    │
              └─────────────────────┘
                        │
                        ▼
              ┌─────────────────────┐
              │ React Query Cache   │
              │ Updated             │
              └─────────────────────┘
                        │
                        ▼
              ┌─────────────────────┐
              │ Components Re-render│
              │ with Fresh Data     │
              └─────────────────────┘
```

---

## Technology Stack Summary

```
┌─────────────────────────────────────────────────┐
│              Frontend Layer                     │
├─────────────────────────────────────────────────┤
│ React 18.2.0                                    │
│ Vite 5.0.0                                      │
│ @tanstack/react-query 5.90.7                   │
│ Axios 1.13.2                                    │
│ Laravel Echo 1.16.1          ◄── NEW (V1.2)    │
│ Pusher JS 8.4.0-rc2          ◄── NEW (V1.2)    │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│             Backend Layer                       │
├─────────────────────────────────────────────────┤
│ Laravel 12.37.0                                 │
│ PHP 8.2-fpm                                     │
│ Laravel Reverb 1.6.0         ◄── NEW (V1.2)    │
│ Guzzle HTTP 7.x                                 │
│ PostgreSQL Driver (pdo_pgsql)                   │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│              Data Layer                         │
├─────────────────────────────────────────────────┤
│ PostgreSQL 15-alpine                            │
│ Eloquent ORM                                    │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│            AI/ML Layer                          │
├─────────────────────────────────────────────────┤
│ Python 3.11-slim                                │
│ FastAPI 0.104.1                                 │
│ Uvicorn 0.24.0                                  │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│         Infrastructure Layer                    │
├─────────────────────────────────────────────────┤
│ Docker Compose                                  │
│ Nginx Alpine                                    │
│ Docker Bridge Network                           │
└─────────────────────────────────────────────────┘
```

---

**Architecture Version:** V1.3
**Last Updated:** November 8, 2025
**Status:** Production Ready
